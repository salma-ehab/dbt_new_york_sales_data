version: 2

models:
  - name: query_4_count_buildings_no_by_different_dimensions
    description: '{{ doc("query_4_description") }}'

    columns:
          - name: borough_name
            description: "The borough's name."

          - name: neighborhood
            description: "The neighborhood's name."

          - name: block
            description: "The block's number."

          - name: building_count
            description: "The count of buildings."